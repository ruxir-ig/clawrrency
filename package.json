{
  "name": "clawrrency",
  "version": "0.1.0",
  "description": "A cooperative digital currency system for AI bots",
  "main": "./packages/sdk/dist/index.js",
  "types": "./packages/sdk/dist/index.d.ts",
  "bin": {
    "clawrrency": "./packages/cli/dist/cli.js"
  },
  "scripts": {
    "build": "cd packages/core && bun run build && cd ../ledger && bun run build && cd ../identity && bun run build && cd ../governance && bun run build && cd ../openclaw && bun run build && cd ../validator && bun run build",
    "test": "bun test",
    "cli": "node packages/cli/dist/cli.js"
  },
  "keywords": [
    "cryptocurrency",
    "blockchain",
    "ai",
    "bots",
    "consensus",
    "pbft"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/ruxir-ig/clawrrency.git"
  }
}
