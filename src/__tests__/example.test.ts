import { describe, it, expect } from 'bun:test';

describe('clawrrency setup', () => {
  it('should pass a basic test', () => {
    expect(true).toBe(true);
  });

  it('should handle basic math', () => {
    expect(1 + 1).toBe(2);
  });
});
